# C revision instructions

Inputs reviewed: initial WorldSpec, deterministic normal/debug renders,
structural critic, and validation report.

1. Move the western-loop sign from `(11,31)` to `(11,33)` so the authored
   branch remains fully traversable while the sign still cues the fork.
2. Move the northern Bellroot ward stone from `(22,19)` to `(22,17)` so it
   frames the clearing instead of blocking the diagonal critical path.
3. Keep the optional loop, secret, NPC counts, encounter allowances, dominant
   landmark count, dimensions, seed, and frozen assets unchanged.
4. Do not follow the critic's suggestion to add a secondary landmark: that
   would change the common functional allowance and make A/B/C less comparable.
5. Revalidate reachability before treating the revision as final.

The initial structural score was 7.5 despite two blocking validation errors.
This is retained as explicit evidence that the structural heuristic cannot
stand in for correctness, visual review, or human enjoyment.
