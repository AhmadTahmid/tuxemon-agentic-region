Design the attached Ashenbell Highlands four-map benchmark through the structured agentic workflow. First write a region reasoning document that commits regional geography, adjacency, shared landmarks, immutable historical facts, NPC knowledge boundaries, ecological gradient, persistent state, optional discovery, and route pacing before any coordinates. Then author one initial WorldSpec against the attached schema and frozen vocabulary. Compile and validate it; review full/debug renders and a transparent consistency critic; issue explicit revision instructions; make exactly one content revision; preserve the initial and final specs plus a unified diff.

The dimensions, topology, creatures, item budget, starter, sprites, tileset, building stamp, state-event grammar, story facts, and functional requirements are fixed by ashenbell_highlands.yaml and must match R0/R1 allowance. Do not change compiler behavior, add art, create a northern destination map, or make the quarry mandatory. Do not calculate an overall world-quality score.
